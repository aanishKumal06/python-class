# Tuples are immutable lists
coordinates = (3, 5)
colors = ("red", "green", "blue")

# Accessing elements (same as lists)
print(coordinates[0])  # 3
print(colors[1:3])     # ("green", "blue")

# Tuples cannot be changed
# coordinates[0] = 4  # This would cause an error!
